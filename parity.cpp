#include <bits/stdc++.h>
using namespace std;

int main(void) {
  int t;
  cin >> t;
  while (t--) {
    int x, y;
    cin >> x;
    (x % 2 == 0) ? cout << "YES" << endl : cout << "NO" << endl;
  }
  return 0;
}

